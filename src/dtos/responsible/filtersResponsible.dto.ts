import { EResponsibleType } from '../../utils/ETypes';

export class FiltersResponsibleDTO {
  firstName?: number;
  lastName?: string;
  email?: string;
  phone?: string;
  type?: EResponsibleType;
}
